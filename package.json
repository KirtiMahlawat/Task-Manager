{
  "name": "team-task-manager",
  "version": "1.0.0",
  "description": "Team Task Manager - Full Stack Web App",
  "scripts": {
    "install:frontend": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "dev:backend": "cd backend && python -m uvicorn main:app --reload --port 3000",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
